local _ = require("gettext")

return {
    fullname = _("Zen MTP"),
    description = _([[Switches compatible Kindle devices to MTP mode and installs a bundled ZenMTP payload to /mnt/us/documents/ZenMTP on first run.]]),
}
